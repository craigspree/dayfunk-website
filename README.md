# DayFunk Website

Landing page for DayFunk — free daytime House Music events in Minneapolis.

Built with plain HTML/CSS. No build step, no dependencies. Deploys via Cloudflare Pages.

## Files

- `index.html` — the site
- `dayfunk-logo.png` — placeholder logo (replace with hi-res asset)

Every push to `main` auto-deploys.